#!/usr/bin/env sh
. "$(dirname -- "$0")/_/husky.sh"

# Fast checks only - pre-commit
echo "🔍 Pre-commit checks..."

# Lint staged files only
npx lint-staged

# Typecheck (fast)
npx tsc --noEmit --skipLibCheck

echo "✅ Pre-commit passed"